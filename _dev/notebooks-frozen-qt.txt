# Frozen Qt-curated notebooks — the "GUI marketing path."
#
# These notebooks carry hand-curated screenshots of the desktop
# ``MetalGUI`` Qt window (full window grabs: title bar, toolbar, dock
# panels, log dock, matplotlib canvas). The published docs site relies
# on them to show what the desktop experience looks like.
#
# CI policy
# ---------
#
# These notebooks are still executed on every PR by ``_dev/rerun_auto.py``
# (and the ``tests-lite`` CI job), BUT with ``--check-only`` semantics:
#
#   - Execution writes outputs to ``/tmp/<basename>.executed.ipynb``,
#     NOT back into the source file.
#   - Pass/fail is gated. The hand-curated outputs in git remain
#     untouched.
#
# That gives us the safety net (CI tells us if a notebook stopped
# running cleanly) without clobbering the rich Qt screenshots with the
# headless ``MetalGUIHeadless`` matplotlib renders that CI produces.
#
# Maintainer workflow
# -------------------
#
# When ``MetalGUI`` changes meaningfully (toolbar layout, theme,
# canvas styling) and the committed screenshots no longer reflect
# current behaviour:
#
#   1. Open the affected notebook locally in JupyterLab with
#      ``pip install "quantum-metal[gui]"`` active.
#   2. ``Restart Kernel and Run All`` — the real desktop ``MetalGUI``
#      paints into a real Qt window; ``gui.screenshot()`` grabs it.
#   3. Visually inspect; the maintainer-arranged dock layout produces
#      noticeably nicer screenshots than a default Qt session.
#   4. Sync: ``uv run python _dev/sync_two_folders.py --write``.
#   5. Commit.
#
# Future option (Option E in the PR thread)
# -----------------------------------------
#
# A manually-triggered ``workflow_dispatch`` CI job using ``Xvfb`` +
# ``quantum-metal[gui]`` can auto-refresh these notebooks in CI. The
# trade-off is that Qt session dock geometry defaults to an uglier
# layout than a hand-arranged maintainer session — auto-refreshed
# screenshots are functional but visually inferior. Deferred to a
# follow-up.
#
# STATUS 2026-08-07: the infra side is built and verified —
# ``.github/workflows/notebooks-qt-refresh.yml`` (workflow_dispatch only,
# never runs on push/PR, uploads an artifact rather than committing) and
# ``_dev/rerun_auto.py --write-frozen``, which flips a frozen-Qt notebook
# from "headless, check-only" to "real MetalGUI under Xvfb, write outputs
# back" — the two are coupled so a headless render can never silently land
# over a hand-curated Qt screenshot. Confirmed end-to-end against a
# throwaway notebook: ``QISKIT_METAL_HEADLESS`` correctly unset,
# ``qm.gui(design)`` resolves to the real ``MetalGUI`` class, not the
# headless viewer.
#
# NOT done: dark-theme screenshots. The theme is deliberately not
# auto-applied on a fresh profile (see
# ``docs/architecture/gui_crash_defenses.md`` — the auto-apply fix was
# tried and reintroduced a segfault, then reverted). A CI runner is always
# a fresh profile, so today this workflow would produce *unstyled*
# screenshots. The safe fix is a `gui.load_stylesheet('metal_dark')` call
# added to each frozen notebook's setup cell — that's a genuine content
# edit to a hand-curated notebook, gated behind the same maintainer
# decision as the rest of the screenshot regen (autoscale default, etc.).
# Do this together with the real regen, not separately.

# --- Section 1: the user-facing onboarding path ---
docs/tut/1-Overview/1.1-Quick-start.ipynb
docs/tut/1-Overview/1.2-Bird's-eye-view-of-Quantum-Metal.ipynb
docs/tut/1-Overview/1.3-Build-a-4-qubit-chip.ipynb

# --- Section 2: lifecycle + the full-chip showpiece ---
docs/tut/2-From-components-to-chip/2.01-How-to-use-a-QComponent.ipynb
docs/tut/2-From-components-to-chip/2.21-Design-a-4-qubit-full-chip.ipynb
